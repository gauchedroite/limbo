// Place your settings in this file to overwrite default and user settings.
{
    // Configure glob patterns for excluding files and folders.
    "files.exclude": {
        "**/.git": true,
        "**/*.js.map": true,
        "**/*.js": { "when": "$(basename).ts" },
        "app/*.js": { "when": "$(basename).tsx" },
        "app.js": { "when": "$(basename).tsx" }
    }
}
